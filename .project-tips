# sccs v2.0 | Claude Code Sync Tool (UV)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
▸ Setup:      uv venv && venv+ && uv pip install -e ".[dev]"
▸ Sync:       sccs sync --dry-run
▸ Status:     sccs status
▸ Categories: sccs categories --all
▸ Enable:     sccs categories enable <name>
▸ Config:     sccs config show
▸ Test:       pytest --cov=sccs
▸ Format:     black sccs/ tests/ && isort sccs/ tests/
